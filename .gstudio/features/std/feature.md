1. The feature lays down the standard source layout of a ts project and its artifacts: src/src/classes, src/src/types,
   src/src/enums, src/src/functions and src/src/consts, each kept even while empty.

## requires

<deterministic>
    Verify that the .gstudio/project.json file's template.name = "ts"
</deterministic>
